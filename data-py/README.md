# data-py — queued, not built yet

Parallel experimental track: the same 3 datasets, processed with
Python + LangChain Text Splitters (chunking), Sentence Transformers
(embeddings), and ChromaDB (vector search) — for comparison against
the Node.js track in `data/`.

Not started. Waiting on an explicit go-ahead before any code lands here.
See the chat history around 2026-08-22 for the full two-track plan.

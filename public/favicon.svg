<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- The app's own anchor mark (same path as the boot screen's icon in
       index.html), reused verbatim rather than a new design, on a solid
       dark badge so it reads clearly in a browser tab regardless of the
       tab bar's own theme. Replaces the leftover default Vite icon that
       had never actually been swapped out. -->
  <rect width="32" height="32" rx="7" fill="#1B1E1D"/>
  <g transform="translate(4,4)">
    <path d="M12 3v18M12 3a3 3 0 100 6 3 3 0 000-6zM5 12H3a9 9 0 0018 0h-2M5 12a7 7 0 0014 0" fill="none" stroke="#E9E7DE" stroke-width="1.6" stroke-linecap="round"/>
  </g>
</svg>
